name: Build Image

on:
  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - piimage/*

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        variant:
          - hyperpixel2-round
        base_image:
          - https://downloads.raspberrypi.com/raspios_oldstable_lite_armhf/images/raspios_oldstable_lite_armhf-2024-10-28/2024-10-22-raspios-bullseye-armhf-lite.img.xz
    steps:
      - name: Checkout Project Repository
        uses: actions/checkout@v3
        with:
          path: repository
          sparse-checkout: |
            piimage
      - name: Checkout CustomPiOS
        uses: actions/checkout@v2
        with:
          repository: 'guysoft/CustomPiOS'
          path: CustomPiOS
      - name: Download Raspbian Image
        run: cd piimage/src/image && wget -q -c --trust-server-names '${{ matrix.base_image }}'
      - name: Update CustomPiOS path
        working-directory: repository/piimage
        run: ${{ github.workspace }}/CustomPiOS/update-custompios-paths
      - name: Build image
        working-directory: repository/piimage/src
        run: |
          sudo modprobe loop
          sudo bash -x ./build_dist ${{ matrix.variant }}
