info:
  name: device-types
  type: http
  seq: 1

http:
  method: GET
  url: "{{device_types_url}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
