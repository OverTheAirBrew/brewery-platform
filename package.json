{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "release": "turbo build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1"
  },
  "release-it": {
    "git": {
      "requireCleanWorkingDir": false
    }
  },
  "dependencies": {
    "turbo": "^1.10.16"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}